package GaltonParts;

public class Board {
    //this will be the skip list
    //each node will correspond to peg
    //when the marble is checking for pegs, it will first calculate its x, y position, then skip to closest coord in this list
}
